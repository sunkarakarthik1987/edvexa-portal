# Prototype runs fully client-side; no backend required yet.
# Placeholders for when the portal is wired to the Edvexa API.
VITE_API_BASE_URL=http://localhost:8000/api
VITE_TENANT_SLUG=demo-institute
